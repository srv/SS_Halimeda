import os
import cv2
import numpy as np
from skimage.transform import resize
from skimage.io import imread, imshow

# metrics lists (acc precision recall fallout)

# load grey
# load masks

# for thr (0-1) / (0-255)
    # apply thr
    # compare
    # save res to metric lists

# ROC - AUC
# best metrics -> best thr
 

